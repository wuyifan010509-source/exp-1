# 实验完成构想 — 做成什么样算成功？

## 一、实验的最终目标

我们要**构建一套端到端的自动化系统**，它接收智能体白盒信息和历史用户问题，**自动输出 ≤200 字的最优结构化意图描述 P\*={C\*,B\*,R\*}**，使得 11 类意图分类的准确率在 354 条黄金测试集上**显著高于人工描述和 INTENT_GPT 基线**。

### 成功标准（量化指标）

| 指标 | 目标 | 说明 |
|------|------|------|
| 总体 Accuracy | **≥ 人工描述基线 + 3%** | 在 354 条黄金测试集上 |
| 边界 Accuracy | **≥ 人工描述基线 + 5%** | 在 62 条边界困难样本上 |
| 平均 Margin | **≥ 0.15** | Top1 与 Top2 概率差，越大越自信 |
| 字数约束 | **≤ 200 字/Agent** | 所有生成的描述严格满足 |
| 收敛性 | **20 代内收敛** | 适应度曲线趋于稳定 |

### 成功的交付物

```
实验产出/
├── 最优描述集/
│   └── 11 个智能体 × P*={C*,B*,R*}          # 自动生成的最优描述
├── 实验结果/
│   ├── 对比实验表格 (Exp1-Exp4)              # 结构化 vs 自由文本、白盒 vs 随机、演化 vs 无演化、框架 vs 基线
│   ├── 消融实验表格 (Ab1-Ab4)                # 验证 C/B/R 各组件的必要性
│   ├── 混淆矩阵热图 (.png)                   # 11×11 分类混淆可视化
│   ├── 演化曲线 (.png)                       # 适应度随代数的变化
│   └── Bad Cases 报告 (.csv)                 # 仍然分错的边界案例
└── 论文支撑材料/
    ├── 算法伪代码 (LaTeX)                     # Algorithm block
    └── 数学公式定义 (LaTeX)                   # 目标函数、适应度函数
```

---

## 二、系统运行全流程预览

下面用一个**端到端的执行流程**来描述系统做成后"运行是什么样的"：

### Step 0: 准备数据
```
输入:
  ├── data/agents/agent_profiles.json     ← 11 个智能体的白盒信息
  │     包含: name, tools[], system_prompt, rag_metadata
  ├── data/golden_test_set.jsonl          ← 354 条标注数据
  │     格式: {"query": "...", "expected_agent": "...", "is_boundary": true/false}
  └── data/unlabeled_queries.jsonl        ← 1500 条无标注历史日志
```

### Step 1: 白盒初始化 → 生成初始种群
```bash
python -m wyf_exp1.run_whitebox_init
```
**做了什么**：读取每个智能体的工具签名、System Prompt、RAG 元数据，调用 Gemini Pro 生成初始结构化描述。产出 **20 个初始个体**（种群）。

**产出示例** (以"新闻智能体"为例)：
```
[C] 检索宏观政策、市场主线、热门话题等时效性强的财经资讯，覆盖政策解读与行业动态。
[B] 仅处理具有时效性的新闻类查询，不涉及个股走势预测或投资建议。
[R] 不处理：个股推荐、技术分析、持仓诊断、账户操作、交易规则咨询。
```
总计 ≤ 200 字。

### Step 2: 评估初始种群适应度
```bash
# 自动调用 qwen2.5-32b 对 354 条测试集做分类，计算 Accuracy
# F(P) = Accuracy(P, D_val) - λ · max(0, len(P) - 200)
```

### Step 3: 遗传演化（核心循环）
```bash
python -m wyf_exp1.run_evolution --generations 30 --pop_size 20
```
每一代的执行流程：
```
┌─────────────────────────────────────┐
│  ① 锦标赛选择 (Tournament, k=3)     │  选出优秀父代
│  ② 结构保留交叉                      │  交换父代的 C/B/R 槽位
│  ③ LLM 定向变异 (Gemini Pro)         │  根据 Bad Cases 重写特定槽位
│  ④ 适应度评估 (qwen2.5-32b)          │  在验证集上计算准确率
│  ⑤ 精英保留                          │  保留 Top-2 最优个体
│  ⑥ 日志记录                          │  最优/平均适应度、种群多样性
└─────────────────────────────────────┘
```
**每隔 5 代**：运行 Margin Sampling，从 1500 条无标注数据中挖掘新的边界样本，注入下一代变异过程。

### Step 4: 运行对比 & 消融实验
```bash
python -m wyf_exp1.experiments.run_comparison   # Exp1-Exp4
python -m wyf_exp1.experiments.run_ablation     # Ab1-Ab4
```

### Step 5: 生成报告
```bash
python -m wyf_exp1.experiments.visualize
```
自动产出混淆矩阵、演化曲线、对比表格、Bad Cases 报告。

---

## 三、关键技术实现细节

### 3.1 结构化描述 P={C,B,R} 的具体表现形式

对于 11 个智能体，每个智能体的描述被严格格式化为三个槽位：

```
[核心能力] <C 文本，描述该智能体主要负责什么>
[处理边界] <B 文本，描述条件限制和适用范围>
[拒绝范围] <R 文本，明确列出不处理的内容>
```

**为什么是这三个而不是其他？** → 见 [design_rationale.md](file:///Users/lxy/.gemini/antigravity/brain/b73247f0-bf77-468a-bdbc-543f370792ad/design_rationale.md)

### 3.2 意图分类的具体调用方式

分类器使用 **qwen2.5-32b** (本地部署在 4×L20 上)，推理流程：

```
System Prompt:
  "你是一个意图分类路由器。根据以下智能体描述，判断用户问题应该由哪个智能体处理。
   仅输出智能体名称，不要解释。"

User Prompt:
  "可用智能体及其描述：
   1. 新闻智能体: [C]...[B]...[R]...
   2. 交易智能体: [C]...[B]...[R]...
   ...（共 11 个）
   
   用户问题：{query}
   
   请输出最匹配的智能体名称。"
```

**关键设计决策**：需要 LLM 同时输出 **置信度分布**（各智能体的概率），用于计算 Margin。实现方式：
- **方案 A**（推荐）：让 LLM 输出 JSON 格式 `{"agent": "xxx", "confidence": {"新闻智能体": 0.8, ...}}`
- **方案 B**：利用 logprobs 计算各类别 token 的生成概率

### 3.3 遗传算法的具体参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 种群大小 | 20 | 每代 20 个候选描述集 |
| 代数上限 | 30 | 最多迭代 30 代 |
| 锦标赛 k | 3 | 每次从 3 个中选最优 |
| 交叉率 | 0.7 | 70% 概率进行槽位交叉 |
| 变异率 | 0.3 | 30% 概率触发 LLM 定向变异 |
| 精英保留 | 2 | 每代直接保留 Top-2 |
| λ (字数惩罚) | 100 | 超字数惩罚系数 |
| Margin 阈值 | 0.1 | 低于此值视为边界样本 |
| 变异模型 | Gemini Pro | 执行槽位重写/缩写 |
| 分类模型 | qwen2.5-32b | 执行意图分类评估 |

### 3.4 适应度评估的计算代价估算

- 每次适应度评估 = 用 qwen2.5-32b 对 354 条 Query 做分类 = **354 次推理**
- 每代评估 20 个个体 = **7,080 次推理/代**
- 30 代总计 = **~212,400 次推理**
- qwen2.5-32b 在 4×L20 上的推理速度约 50-100 tokens/s，每次分类约 200 tokens → 约 2-4 秒/条
- **总计约 120-240 小时的 GPU 时间（单卡串行）**

> [!IMPORTANT]
> 这个计算代价较大。优化策略：
> 1. **缩小验证集**：从 354 条中抽取 100 条代表性样本用于适应度评估，完整集仅用于最终评估
> 2. **批量推理**：利用 vLLM 等推理框架批量处理，提升 4-8x 吞吐
> 3. **减少代数**：观察到收敛后提前停止
> 4. **缓存机制**：相同描述-Query 对的分类结果缓存复用

### 3.5 白盒初始化的具体步骤

```
对于每个智能体 agent_i:
  1. 解析 tools[] → 提取函数名和描述 → 聚合为 C 候选
  2. 计算 全局工具集 \ agent_i.tools → 生成 R 候选
  3. 读取 rag_metadata (category, product_line) → 生成 B 候选
  4. 读取 system_prompt → 调 Gemini Pro 压缩为 ≤200 字的 {C,B,R}
  5. 对 C/B/R 做字数分配：C ≤ 80 字, B ≤ 60 字, R ≤ 60 字
  6. 生成 20 个变体（通过微调 Prompt 的措辞和侧重点）
```

### 3.6 LLM 定向变异的 Prompt 模板

```text
你是一个智能体描述优化专家。
当前智能体描述如下：
[核心能力] {current_C}
[处理边界] {current_B}
[拒绝范围] {current_R}

以下用户问题被错误分类：
{bad_cases}

请针对 [{target_slot}] 槽位进行优化，使得上述错误分类被纠正。
要求：
1. 仅修改 [{target_slot}]，不改动其他槽位
2. 修改后 [{target_slot}] 字数 ≤ {slot_max_len} 字
3. 保持语义精确，避免模糊表述
4. 优先使用否定式表述（"不处理..."）来划定边界

请直接输出修改后的 [{target_slot}] 内容：
```

---

## 四、与 INTENT_GPT 基线的核心区别

| 维度 | INTENT_GPT 基线 | 本框架 (P={C,B,R} + GA) |
|------|-----------------|------------------------|
| 描述生成方式 | LLM 根据 few-shot 自由生成 | 结构化槽位 + 遗传搜索 |
| 搜索空间 | 不受限的文本空间 | 三个正交槽位内搜索 |
| 字数控制 | 无硬约束 | 适应度函数内置惩罚项 |
| 边界处理 | 隐式（依赖 LLM 推理） | 显式 R 槽位 + Margin 挖掘 |
| 迭代方式 | 一次性生成 | 遗传演化多代迭代 |
| 可解释性 | 黑盒 Prompt | C/B/R 结构透明 |
